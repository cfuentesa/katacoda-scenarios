## Informaci&oacute;n de Docker CE

Validamos que el servicio Docker esta corriendo de manera correcta con el siguiente comando:

`docker info`{{execute}}

Para conocer la versi&oacute; de Docker que hemos instalado:

`docker -v`{{execute}}
